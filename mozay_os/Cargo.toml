#cargo.toml for mozay_os
[package]
name = "mozay_os"
version = "0.1.0"
edition = "2021"

[dependencies]
bootloader = "0.9.0"
volatile = "0.2.6"
spin = "0.5.0"

[dependencies.lazy_static]
version = "1.0"
features = ["spin_no_std"]